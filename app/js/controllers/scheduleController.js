'use strict';

angular
  .module('distill.controllers.schedule', ['distill.services.login'])
  .controller('ScheduleCtrl', ['$scope', '$location', 'loginService',
    function($scope, $location, loginService){
    }
  ]);

